#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define rep2(i, s, n) for (int i = (s); i < (int)(n); i++)

int main() {
  int R; cin >> R;
  if (R <= 99) {
    cout << 100 - R << endl;
  } else if (R <= 199) {
    cout << 200 - R << endl;
  } else if (R <= 299) {
    cout << 300 - R << endl;
  } else if (R <= 399) {
  }
}

